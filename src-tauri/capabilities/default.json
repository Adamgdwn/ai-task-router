{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell with custom Rust discovery commands. No broad filesystem, shell, process, upload, provider, telemetry, updater, or plugin discovery permissions are approved.",
  "windows": [
    "main"
  ],
  "permissions": []
}
